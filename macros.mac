write macro string, lgsir
local writeloop
	push ax
	push bx
	push cx
	push dx
	mov bx, 0
	mov si,0
	mov cx, lgsir
writeloop:
	mov dl, string[si]
	mov ah, 2
	int 21h
	inc si
	loop writeloop
	pop dx
	pop cx
	pop bx
	pop ax
endm

newline macro
	push ax
	push dx
	mov dl, 10 ;new line
	mov ah, 2
	int 21h	
	pop dx
	pop ax
endm
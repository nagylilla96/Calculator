write macro string, lgsir
local writeloop
	push ax
	push bx
	push cx
	push dx
	mov bx, 0
	mov si,0
	mov cx, lgsir
writeloop:
	mov dl, string[si]
	mov ah, 2
	int 21h
	inc si
	loop writeloop
	pop dx
	pop cx
	pop bx
	pop ax
endm

checknr macro nr
	cmp nr, 30h
	jnge error1
	cmp nr, 39h
	jnle error1
endm